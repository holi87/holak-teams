{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://holak-teams.local/argus/role-manifest.schema.json",
  "title": "Argus canonical role source manifest",
  "type": "object",
  "additionalProperties": false,
  "required": ["$schema", "schemaVersion", "contracts", "roles"],
  "properties": {
    "$schema": { "type": "string" },
    "schemaVersion": { "const": 1 },
    "contracts": {
      "type": "object",
      "additionalProperties": false,
      "required": ["modelPolicy", "ownership", "capabilities", "doctrine", "adapters"],
      "properties": {
        "modelPolicy": { "const": "argus/model-policy.json" },
        "ownership": { "const": "argus/raci.json" },
        "capabilities": { "const": "argus/capabilities/capability-matrix.json" },
        "doctrine": { "const": "argus/shared-skills/qa-doctrine/SKILL.md" },
        "adapters": { "const": "argus/roles/runtime-adapters.json" }
      }
    },
    "roles": {
      "type": "array",
      "minItems": 27,
      "maxItems": 27,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["slug", "source", "displayName", "description", "color", "claudeTools"],
        "properties": {
          "slug": { "type": "string", "pattern": "^[a-z]+(?:-[a-z]+)*$" },
          "source": { "type": "string", "pattern": "^[a-z]+(?:-[a-z]+)*\\.md$" },
          "displayName": { "type": "string", "minLength": 1 },
          "description": { "type": "string", "minLength": 1 },
          "color": { "enum": ["cyan", "red", "green", "yellow", "purple"] },
          "claudeTools": {
            "type": "array",
            "minItems": 1,
            "uniqueItems": true,
            "items": { "type": "string", "minLength": 1 }
          }
        }
      }
    }
  }
}
